[project]
name = "trustforge"
version = "0.1.0"
description = "零信任隔离铸造机 — 防大模型走捷径的强制开发框架"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0", "pyyaml>=6.0"]

[project.scripts]
trustforge = "trustforge.cli:main"
